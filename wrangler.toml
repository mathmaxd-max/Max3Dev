# Cloudflare Pages: point build output to dist/ in the project dashboard.
# Optional local preview: npx wrangler pages dev dist

name = "max3dev"
compatibility_date = "2025-04-01"
pages_build_output_dir = "dist"
